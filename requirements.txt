# backend runtime
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.1
pydantic-settings>=2.2.0
slowapi>=0.1.9

# mcp proxy runtime (also declared in pyproject.toml for pip install)
mcp>=1.0.0
httpx>=0.27.0

# dev / test
pytest>=8.0.0
requests>=2.31.0
